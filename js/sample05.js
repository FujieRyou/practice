for(let i=0; i<10; i++){
    console.log(`<li>list${i+1}</li>`)
    document.getElementById('list').innerHTML += `<li>list${i+1}</li>`;
}

/*
//配列 array
let list = new Array();
list[0] = 10;
list[1] = 20;
list[2] = 30;

//クラスをとってくる
let colorName = document.getElementsByClassName('btn');

//要素を数える
console.log(colorName.length);

//forで返しbuttonを読み込ませる
for(k=0; k<colorName.length; k++){
    document.getElementsByClassName('btn')[k].onclick = (as) =>{
        console.log(as.target.value)
        document.body.style.backgroundColor = as.target.value;
    }
}
*/


let iroNamae







